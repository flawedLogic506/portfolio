/** @jsx jsx */
import {css, jsx} from '@emotion/core';

const Sidebar = () => (
  <div css={css`
    width: 20%;
    background: salmon;
    min-height: 100vh;
  `}>
    Sidebar
  </div>
);

export default Sidebar;